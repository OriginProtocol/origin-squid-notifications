/**
 * Run commands like `BLOCK_FROM=18102886 PROCESSOR=Trace yarn process` to filter down what processors will be run.
 */
// import './compound' // Unused
import './chainlink'
import './curve'
import './origin'
import './prime-eth'

export { processors } from './processors'
