name: Release squid version
on:
  push:
    branches:
      - 'v[0-9]+'

permissions:
  contents: read
  packages: read

jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.24.0

      - name: Install squid CLI
        run: pnpm add -g @subsquid/cli

      - name: Configure GitHub Packages auth
        run: pnpm config set //npm.pkg.github.com/:_authToken "$NODE_AUTH_TOKEN"
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: update squid.yml
        run: |
          # Get the branch name from GitHub
          BRANCH_NAME=${{ github.ref_name }}

          # Extract the version from the branch name (assuming format 'vXX')
          VERSION=$(echo $BRANCH_NAME | sed 's/^v//')

          # Update the version in squid.yaml
          sed -i "s/^version: .*/version: $VERSION/" squid.yaml

          # Optional: Print the updated version for verification
          echo "Updated squid.yaml version to: $VERSION"

      - name: Authenticate to squid
        env:
          API_TOKEN: ${{ secrets.SQUID_API_TOKEN }}
        run: sqd auth -k $API_TOKEN

      # SQD's build container runs its own install, so the uploaded bundle needs
      # registry auth in .npmrc. Appended here and restored on exit so the token
      # never lands in a committed file.
      - name: Build and deploy squid
        env:
          NODE_AUTH_TOKEN: ${{ github.token }}
        run: |
          cp .npmrc .npmrc.ci-backup
          trap 'mv .npmrc.ci-backup .npmrc' EXIT
          printf '\n//npm.pkg.github.com/:_authToken=%s\n' "$NODE_AUTH_TOKEN" >> .npmrc

          sqd build && sqd deploy . -o origin --no-stream-logs --allow-update
